[pytest]
testpaths = tests
addopts = -ra -q
markers =
    uses_real_delivery_lock: exercises the production final-delivery lock instead of test bypasses
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
