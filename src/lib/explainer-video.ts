/**
 * Homepage explainer video — single replacement point for the portal surface.
 *
 * TEMPORARY DEVELOPMENT STATE: `src` is intentionally blank until Moses
 * supplies the completed Mobi explainer. To publish it this weekend, copy the
 * optimized MP4 into `public/video/` and set `src` below (for example,
 * `/video/mobi-explainer.mp4`). Optionally set a Mobi-owned `poster` image.
 * Do not place a stock video or third-party tracking embed here.
 */
export const EXPLAINER_VIDEO = {
  src: "",
  poster: "",
  title: "See How Mobi Adds Estimating Capacity Without Another Full-Time Hire",
  description:
    "See how contractors move from project documents to a detailed, human-reviewed estimate without adding another full-time estimator.",
  placeholderLabel: "Temporary preview — final Mobi explainer video coming soon",
} as const;
